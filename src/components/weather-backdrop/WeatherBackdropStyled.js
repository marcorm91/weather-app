import styled from 'styled-components'

export const WeatherBackdropStyled = styled.div`
    position: fixed;
    inset: var(--wa-spacing-00);
    background-color: var(--wa-black-op50);
    z-index: 998;   
`